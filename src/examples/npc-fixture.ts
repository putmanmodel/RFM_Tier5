import { WaveNode }         from '../types/WaveNode'
import { EmotionalProfile } from '../types/EmotionalProfile'

export const npc = {
  waves: [] as WaveNode[],
  profile: {} as EmotionalProfile,
  currentState: '' as string
}
